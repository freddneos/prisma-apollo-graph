# By default, Heroku prunes the devDependencies when the server is launched. 
# This is problematic as the server needs to use the rimraf script 
# for the build script. Therefore, you need to add an ENV variable 
# in the Heroku app called NPM_CONFIG_PRODUCTION=false. 
# Go to the Settings tab and add a Config Vars:
web: npm start